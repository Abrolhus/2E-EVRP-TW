use crate::instance::Instance;
use crate::ev_route::EvRoute;
pub struct Solution{
    satelites: Vec<Satelites>,
    cost: f64,
    n_trucks: u32,
    n_evs: u32,

    //truck_routes: Vec<TruckRute>,

}
impl Solution{
}
